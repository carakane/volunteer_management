<%= form_for @opportunity do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :status %>
  <%= f.text_field :status %>
  <%= f.hidden_field :organization_id %>
  <!-- :organization_id, :volunteer_id -->
  <%= f.submit %>
<% end %>
