Name: <%= link_to opportunity.name.titleize, organization_opportunity_path(opportunity.organization, opportunity) %> ||
Organization: <%= link_to opportunity.organization.name.titleize, organization_path(opportunity.organization) %> ||
Status: <%= opportunity.status.titleize %> ||
Day: <%= opportunity.day.titleize %><br />
Volunteer:
<% if opportunity.volunteer.present? %>
  <%= link_to opportunity.volunteer.name, volunteer_path(opportunity.volunteer) %>
<% else %>
  Not Assigned
<% end %>
  <br />
<br />
