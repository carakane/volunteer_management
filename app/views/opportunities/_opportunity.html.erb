Opportunities:
<% @opportunities.each do |opportunity| %>
  <%= link_to opportunity.name, organization_opportunity_path(opportunity.organization, opportunity) %> ||
  <%= link_to opportunity.organization.name, organization_path(opportunity.organization) %> ||
  <%= opportunity.status %> ||
  <%= opportunity.day %>
<% end %>
